{
  "name": "saude-app-tcc",
  "version": "1.0.0",
  "description": "TCC Health App Backend",
  "main": "server.js",
  "scripts": {
    "sync": "node src/config/sync.js",
    "start": "npm run sync && node server.js",
    "dev": "npm run sync && nodemon server.js",
    "test": "jest"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-cassandra": "^2.9.1",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "nodemon": "^3.1.14",
    "supertest": "^7.2.2"
  }
}
