import React, { useState } from "react";
import { Alert, View } from "react-native";
import Screen from "../../components/Screen"; import AppHeader from "../../components/AppHeader"; import AppInput from "../../components/AppInput"; import PrimaryButton from "../../components/PrimaryButton"; import { resetPassword } from "../../services/authService";
export default function ResetPasswordScreen({ navigation, route }) { const [password, setPassword] = useState(""); const [confirm, setConfirm] = useState(""); const [loading, setLoading] = useState(false); async function submit() { if (password.length < 6) return Alert.alert("Senha", "Use no mínimo 6 caracteres."); if (password !== confirm) return Alert.alert("Senha", "As senhas não coincidem."); setLoading(true); try { await resetPassword(route.params?.userId, password); Alert.alert("Senha atualizada", "Agora você já pode entrar com a nova senha."); navigation.popToTop(); } catch(e) { Alert.alert("Erro", e.message); } finally { setLoading(false); } } return <Screen><View className="mx-auto w-full max-w-[520px]"><AppHeader title="Atualizar senha" subtitle="Crie uma nova senha e confirme antes de enviar." onBack={() => navigation.navigate("Login")}/><View className="rounded-3xl border border-mint-100 bg-white p-5 hover:shadow-xl transition-all duration-200"><AppInput label="Nova senha" value={password} onChangeText={setPassword} secureTextEntry placeholder="Mínimo 6 caracteres"/><AppInput label="Confirmar senha" value={confirm} onChangeText={setConfirm} secureTextEntry placeholder="Repita a nova senha"/><PrimaryButton title="Enviar" onPress={submit} loading={loading}/></View></View></Screen>; }
