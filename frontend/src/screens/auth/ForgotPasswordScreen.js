import React, { useState } from "react";
import { Alert, Text, View } from "react-native";
import Screen from "../../components/Screen"; import AppHeader from "../../components/AppHeader"; import AppInput from "../../components/AppInput"; import PrimaryButton from "../../components/PrimaryButton"; import { sendResetEmail } from "../../services/authService";
export default function ForgotPasswordScreen({ navigation }) { const [email, setEmail] = useState(""); const [loading, setLoading] = useState(false); async function submit() { if (!email.trim()) return Alert.alert("E-mail", "Informe seu e-mail."); setLoading(true); try { const result = await sendResetEmail(email.trim()); Alert.alert("E-mail enviado", "No mock, consideramos que o link de redefinição foi enviado."); navigation.navigate("ResetPassword", { userId: result.userId }); } catch(e) { Alert.alert("Erro", e.message); } finally { setLoading(false); } } return <Screen><View className="mx-auto w-full max-w-[520px]"><AppHeader title="Esqueci minha senha" subtitle="Informe o e-mail cadastrado para simular o envio do link de redefinição." onBack={() => navigation.goBack()}/><View className="rounded-3xl border border-mint-100 bg-white p-5 hover:shadow-xl transition-all duration-200"><AppInput label="E-mail" value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" inputMode="email" placeholder="voce@email.com"/><PrimaryButton title="Enviar e-mail" onPress={submit} loading={loading}/></View></View></Screen>; }
